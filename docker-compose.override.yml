services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    volumes:
        -   .:/var/www
    environment:
        APP_ENV: dev

  nginx:
      volumes:
          - .:/var/www
          - ./docker/nginx/proxy.conf:/etc/nginx/conf.d/default.conf
